// oxfmt-ignore
const guardTranslations = {"en":{"Actual upstream model":"Actual upstream model","Add rule":"Add rule","All detected requests on the same channel share one count across models and groups. A matching request resets the count; missing model names are skipped.":"All detected requests on the same channel share one count across models and groups. A matching request resets the count; missing model names are skipped.","Allowed upstream models":"Allowed upstream models","Allowlisted channel IDs":"Allowlisted channel IDs","Allowlisted channels skip detection, counting, records, disabling, and notifications.":"Allowlisted channels skip detection, counting, records, disabling, and notifications.","Available channels":"Available channels","Channel":"Channel","Channel allowlist":"Channel allowlist","Channel disabled":"Channel disabled","Channel status":"Channel status","Complete groups and model names for rule {{number}}.":"Complete groups and model names for rule {{number}}.","Consecutive mismatch threshold":"Consecutive mismatch threshold","Consecutive mismatches":"Consecutive mismatches","Detected at":"Detected at","Detection enabled":"Detection enabled","Enter an integer from 1 to 100.":"Enter an integer from 1 to 100.","Enter positive integer channel IDs.":"Enter positive integer channel IDs.","Expected upstream models":"Expected upstream models","Failed to load channels":"Failed to load channels","Group":"Group","Loading channels...":"Loading channels...","Loading records...":"Loading records...","Loading settings...":"Loading settings...","Model guard settings saved":"Model guard settings saved","Model matching rules":"Model matching rules","Model mismatch records":"Model mismatch records","Next page":"Next page","No allowlisted channels":"No allowlisted channels","No channels found":"No channels found","No groups available":"No groups available","No model matching rules":"No model matching rules","No model mismatch records":"No model mismatch records","Not disabled":"Not disabled","Notification Center":"Notification Center","One exact model name per line":"One exact model name per line","Previous page":"Previous page","Refresh records":"Refresh records","Reload settings":"Reload settings","Remove":"Remove","Remove {{channel}} from allowlist":"Remove {{channel}} from allowlist","Remove rule":"Remove rule","Request failed":"Request failed","Request model":"Request model","Retry channel search":"Retry channel search","Rule {{number}}":"Rule {{number}}","Rule enabled":"Rule enabled","Save settings":"Save settings","Saving...":"Saving...","Search":"Search","Search channels":"Search channels","Select no more than 1000 channels.":"Select no more than 1000 channels.","Selected {{count}} of 1000":"Selected {{count}} of 1000","Selected groups":"Selected groups","Separate IDs with commas or newlines. Duplicates are removed. Clear the field to remove all allowlisted channels, then save settings.":"Separate IDs with commas or newlines. Duplicates are removed. Clear the field to remove all allowlisted channels, then save settings.","Settings changed elsewhere. Reload before saving again.":"Settings changed elsewhere. Reload before saving again.","Total {{count}}":"Total {{count}}","Unavailable channel":"Unavailable channel","Unavailable group":"Unavailable group","Upstream model guard":"Upstream model guard"},"zh":{"Actual upstream model":"实测上游模型","Add rule":"添加规则","All detected requests on the same channel share one count across models and groups. A matching request resets the count; missing model names are skipped.":"同一渠道的所有检测请求跨模型、跨分组共用计数。匹配请求清零；缺少模型名的请求跳过。","Allowed upstream models":"允许的上游模型","Allowlisted channel IDs":"渠道白名单 ID","Allowlisted channels skip detection, counting, records, disabling, and notifications.":"白名单渠道跳过检测、计数、异常记录、关闭和通知。","Available channels":"可选渠道","Channel":"渠道","Channel allowlist":"渠道白名单","Channel disabled":"渠道已关闭","Channel status":"渠道状态","Complete groups and model names for rule {{number}}.":"请补全规则 {{number}} 的分组和模型名称。","Consecutive mismatch threshold":"连续异常阈值","Consecutive mismatches":"连续异常次数","Detected at":"异常时间","Detection enabled":"启用检测","Enter an integer from 1 to 100.":"请输入 1 至 100 的整数。","Enter positive integer channel IDs.":"请输入正整数渠道 ID。","Expected upstream models":"预期上游模型","Failed to load channels":"渠道加载失败","Group":"分组","Loading channels...":"正在加载渠道...","Loading records...":"正在加载记录...","Loading settings...":"正在加载设置...","Model guard settings saved":"模型校验配置已保存","Model matching rules":"模型匹配规则","Model mismatch records":"模型异常记录","Next page":"下一页","No allowlisted channels":"暂无白名单渠道","No channels found":"未找到渠道","No groups available":"暂无可用分组","No model matching rules":"暂无模型匹配规则","No model mismatch records":"暂无模型异常记录","Not disabled":"未关闭","Notification Center":"通知中心","One exact model name per line":"每行一个完整模型名称","Previous page":"上一页","Refresh records":"刷新记录","Reload settings":"重新加载配置","Remove":"移除","Remove {{channel}} from allowlist":"将 {{channel}} 移出白名单","Remove rule":"移除规则","Request failed":"请求失败","Request model":"请求模型","Retry channel search":"重新加载渠道","Rule {{number}}":"规则 {{number}}","Rule enabled":"启用规则","Save settings":"保存设置","Saving...":"正在保存...","Search":"搜索","Search channels":"搜索渠道","Select no more than 1000 channels.":"最多可选择 1000 个渠道。","Selected {{count}} of 1000":"已选 {{count}} / 1000","Selected groups":"已选组","Separate IDs with commas or newlines. Duplicates are removed. Clear the field to remove all allowlisted channels, then save settings.":"使用逗号或换行分隔，重复 ID 自动去重。清空输入并保存设置可移除全部白名单渠道。","Settings changed elsewhere. Reload before saving again.":"配置已被其他会话修改，请重新加载后再保存。","Total {{count}}":"共 {{count}} 条","Unavailable channel":"不可用渠道","Unavailable group":"不可用分组","Upstream model guard":"上游模型校验"},"zh-TW":{"Actual upstream model":"實測上游模型","Add rule":"新增規則","All detected requests on the same channel share one count across models and groups. A matching request resets the count; missing model names are skipped.":"同一通道的所有偵測請求跨模型、跨群組共用計數。符合預期的請求歸零；缺少模型名稱的請求略過。","Allowed upstream models":"允許的上游模型","Allowlisted channel IDs":"渠道白名單 ID","Allowlisted channels skip detection, counting, records, disabling, and notifications.":"允許清單中的通道略過偵測、計數、異常紀錄、停用和通知。","Available channels":"可選通道","Channel":"渠道","Channel allowlist":"通道允許清單","Channel disabled":"通道已停用","Channel status":"通道狀態","Complete groups and model names for rule {{number}}.":"請補齊規則 {{number}} 的群組和模型名稱。","Consecutive mismatch threshold":"連續異常門檻","Consecutive mismatches":"連續異常次數","Detected at":"異常時間","Detection enabled":"啟用偵測","Enter an integer from 1 to 100.":"請輸入 1 至 100 的整數。","Enter positive integer channel IDs.":"請輸入正整數渠道 ID。","Expected upstream models":"預期上游模型","Failed to load channels":"通道載入失敗","Group":"分組","Loading channels...":"正在載入通道...","Loading records...":"正在載入紀錄...","Loading settings...":"正在載入設定...","Model guard settings saved":"模型校驗設定已儲存","Model matching rules":"模型比對規則","Model mismatch records":"模型異常紀錄","Next page":"下一頁","No allowlisted channels":"尚無允許清單通道","No channels found":"找不到通道","No groups available":"暫無可用群組","No model matching rules":"暫無模型比對規則","No model mismatch records":"暫無模型異常紀錄","Not disabled":"未停用","Notification Center":"通知中心","One exact model name per line":"每行一個完整模型名稱","Previous page":"上一頁","Refresh records":"重新整理紀錄","Reload settings":"重新載入設定","Remove":"移除","Remove {{channel}} from allowlist":"將 {{channel}} 移出允許清單","Remove rule":"移除規則","Request failed":"請求失敗","Request model":"請求模型","Retry channel search":"重新載入通道","Rule {{number}}":"規則 {{number}}","Rule enabled":"啟用規則","Save settings":"儲存設定","Saving...":"正在儲存...","Search":"搜尋","Search channels":"搜尋通道","Select no more than 1000 channels.":"最多可選擇 1000 個通道。","Selected {{count}} of 1000":"已選 {{count}} / 1000","Selected groups":"已選群組","Separate IDs with commas or newlines. Duplicates are removed. Clear the field to remove all allowlisted channels, then save settings.":"使用逗號或換行分隔，重複 ID 會自動去重。清空輸入並儲存設定可移除全部白名單渠道。","Settings changed elsewhere. Reload before saving again.":"設定已被其他工作階段修改，請重新載入後再儲存。","Total {{count}}":"共 {{count}} 筆","Unavailable channel":"不可用通道","Unavailable group":"無法使用的群組","Upstream model guard":"上游模型校驗"},"fr":{"Actual upstream model":"Modèle amont observé","Add rule":"Ajouter une règle","All detected requests on the same channel share one count across models and groups. A matching request resets the count; missing model names are skipped.":"Les requêtes contrôlées d’un même canal partagent un compteur, tous modèles et groupes confondus. Une correspondance le remet à zéro ; les noms de modèle absents sont ignorés.","Allowed upstream models":"Modèles amont autorisés","Allowlisted channel IDs":"ID des canaux autorisés","Allowlisted channels skip detection, counting, records, disabling, and notifications.":"Les canaux exemptés ignorent la détection, le comptage, les journaux, la désactivation et les notifications.","Available channels":"Canaux disponibles","Channel":"Canal","Channel allowlist":"Canaux exemptés","Channel disabled":"Canal désactivé","Channel status":"État du canal","Complete groups and model names for rule {{number}}.":"Complétez les groupes et modèles de la règle {{number}}.","Consecutive mismatch threshold":"Seuil de divergences consécutives","Consecutive mismatches":"Divergences consécutives","Detected at":"Détecté le","Detection enabled":"Détection active","Enter an integer from 1 to 100.":"Saisissez un entier de 1 à 100.","Enter positive integer channel IDs.":"Saisissez des ID de canal entiers positifs.","Expected upstream models":"Modèles amont attendus","Failed to load channels":"Échec du chargement des canaux","Group":"Groupe","Loading channels...":"Chargement des canaux...","Loading records...":"Chargement des enregistrements...","Loading settings...":"Chargement des paramètres...","Model guard settings saved":"Paramètres de vérification enregistrés","Model matching rules":"Règles de correspondance","Model mismatch records":"Divergences de modèles","Next page":"Page suivante","No allowlisted channels":"Aucun canal exempté","No channels found":"Aucun canal trouvé","No groups available":"Aucun groupe disponible","No model matching rules":"Aucune règle de correspondance","No model mismatch records":"Aucune divergence de modèle","Not disabled":"Non désactivé","Notification Center":"Centre de notifications","One exact model name per line":"Un nom de modèle exact par ligne","Previous page":"Page précédente","Refresh records":"Actualiser les enregistrements","Reload settings":"Recharger les paramètres","Remove":"Retirer","Remove {{channel}} from allowlist":"Retirer {{channel}} des exemptions","Remove rule":"Retirer la règle","Request failed":"Échec de la requête","Request model":"Modèle demandé","Retry channel search":"Réessayer la recherche","Rule {{number}}":"Règle {{number}}","Rule enabled":"Règle active","Save settings":"Enregistrer les paramètres","Saving...":"Enregistrement en cours...","Search":"Rechercher","Search channels":"Rechercher des canaux","Select no more than 1000 channels.":"Sélectionnez au maximum 1000 canaux.","Selected {{count}} of 1000":"{{count}} sélectionnés sur 1000","Selected groups":"Groupes sélectionnés","Separate IDs with commas or newlines. Duplicates are removed. Clear the field to remove all allowlisted channels, then save settings.":"Séparez les ID par des virgules ou des retours à la ligne. Les doublons sont supprimés. Videz le champ puis enregistrez pour retirer tous les canaux de la liste.","Settings changed elsewhere. Reload before saving again.":"Les paramètres ont changé ailleurs. Rechargez avant de sauvegarder.","Total {{count}}":"Total : {{count}}","Unavailable channel":"Canal indisponible","Unavailable group":"Groupe indisponible","Upstream model guard":"Vérification des modèles amont"},"ja":{"Actual upstream model":"実際の上流モデル","Add rule":"ルールを追加","All detected requests on the same channel share one count across models and groups. A matching request resets the count; missing model names are skipped.":"同じチャネルの検出対象リクエストは、モデルやグループをまたいでカウントを共有します。一致したリクエストでゼロに戻り、モデル名がない場合はスキップします。","Allowed upstream models":"許可する上流モデル","Allowlisted channel IDs":"許可リストのチャネル ID","Allowlisted channels skip detection, counting, records, disabling, and notifications.":"許可リストのチャネルは検出、カウント、記録、無効化、通知をスキップします。","Available channels":"選択可能なチャネル","Channel":"チャネル","Channel allowlist":"チャネル許可リスト","Channel disabled":"チャネル無効化済み","Channel status":"チャネル状態","Complete groups and model names for rule {{number}}.":"ルール {{number}} のグループとモデル名を入力してください。","Consecutive mismatch threshold":"連続不一致のしきい値","Consecutive mismatches":"連続不一致回数","Detected at":"検出日時","Detection enabled":"検出を有効化","Enter an integer from 1 to 100.":"1～100 の整数を入力してください。","Enter positive integer channel IDs.":"チャネル ID は正の整数で入力してください。","Expected upstream models":"期待する上流モデル","Failed to load channels":"チャネルを読み込めませんでした","Group":"グループ","Loading channels...":"チャネルを読み込み中...","Loading records...":"記録を読み込み中...","Loading settings...":"設定をロード中...","Model guard settings saved":"モデル検証設定を保存しました","Model matching rules":"モデル照合ルール","Model mismatch records":"モデル不一致の記録","Next page":"次のページ","No allowlisted channels":"許可リストは空です","No channels found":"チャネルが見つかりません","No groups available":"利用可能なグループがありません","No model matching rules":"モデル照合ルールがありません","No model mismatch records":"モデル不一致の記録がありません","Not disabled":"無効化されていません","Notification Center":"通知センター","One exact model name per line":"1 行に 1 つの正確なモデル名","Previous page":"前のページ","Refresh records":"記録を更新","Reload settings":"設定を再読み込み","Remove":"削除","Remove {{channel}} from allowlist":"{{channel}} を許可リストから削除","Remove rule":"ルールを削除","Request failed":"リクエスト失敗","Request model":"要求モデル","Retry channel search":"チャネル検索を再試行","Rule {{number}}":"ルール {{number}}","Rule enabled":"ルールを有効化","Save settings":"設定を保存","Saving...":"保存中...","Search":"検索","Search channels":"チャネルを検索","Select no more than 1000 channels.":"選択できるチャネルは最大 1000 件です。","Selected {{count}} of 1000":"{{count}} / 1000 件選択","Selected groups":"選択したグループ","Separate IDs with commas or newlines. Duplicates are removed. Clear the field to remove all allowlisted channels, then save settings.":"ID はカンマまたは改行で区切ります。重複は削除されます。すべてのチャネルを許可リストから削除するには、入力欄を空にして設定を保存してください。","Settings changed elsewhere. Reload before saving again.":"設定が別の場所で変更されました。再読み込みしてから保存してください。","Total {{count}}":"合計 {{count}}","Unavailable channel":"利用できないチャネル","Unavailable group":"利用できないグループ","Upstream model guard":"上流モデル検証"},"ru":{"Actual upstream model":"Фактическая модель провайдера","Add rule":"Добавить правило","All detected requests on the same channel share one count across models and groups. A matching request resets the count; missing model names are skipped.":"Все проверяемые запросы одного канала используют общий счётчик для всех моделей и групп. Совпадение сбрасывает счётчик; запросы без имени модели пропускаются.","Allowed upstream models":"Разрешённые модели провайдера","Allowlisted channel IDs":"ID каналов в белом списке","Allowlisted channels skip detection, counting, records, disabling, and notifications.":"Исключённые каналы пропускают проверку, подсчёт, записи, отключение и уведомления.","Available channels":"Доступные каналы","Channel":"Канал","Channel allowlist":"Исключённые каналы","Channel disabled":"Канал отключён","Channel status":"Состояние канала","Complete groups and model names for rule {{number}}.":"Укажите группы и модели для правила {{number}}.","Consecutive mismatch threshold":"Порог последовательных несовпадений","Consecutive mismatches":"Последовательные несовпадения","Detected at":"Время обнаружения","Detection enabled":"Проверка включена","Enter an integer from 1 to 100.":"Введите целое число от 1 до 100.","Enter positive integer channel IDs.":"Введите положительные целые ID каналов.","Expected upstream models":"Ожидаемые модели провайдера","Failed to load channels":"Не удалось загрузить каналы","Group":"Группа","Loading channels...":"Загрузка каналов...","Loading records...":"Загрузка записей...","Loading settings...":"Загрузка настроек...","Model guard settings saved":"Настройки проверки моделей сохранены","Model matching rules":"Правила сопоставления моделей","Model mismatch records":"Записи несовпадений моделей","Next page":"Следующая страница","No allowlisted channels":"Нет исключённых каналов","No channels found":"Каналы не найдены","No groups available":"Нет доступных групп","No model matching rules":"Нет правил сопоставления моделей","No model mismatch records":"Нет записей о несовпадениях моделей","Not disabled":"Не отключён","Notification Center":"Центр уведомлений","One exact model name per line":"Одно точное имя модели в строке","Previous page":"Предыдущая страница","Refresh records":"Обновить записи","Reload settings":"Перезагрузить настройки","Remove":"Удалить","Remove {{channel}} from allowlist":"Убрать {{channel}} из исключений","Remove rule":"Удалить правило","Request failed":"Запрос не выполнен","Request model":"Запрошенная модель","Retry channel search":"Повторить поиск каналов","Rule {{number}}":"Правило {{number}}","Rule enabled":"Правило включено","Save settings":"Сохранить настройки","Saving...":"Сохранение...","Search":"Поиск","Search channels":"Поиск каналов","Select no more than 1000 channels.":"Можно выбрать не более 1000 каналов.","Selected {{count}} of 1000":"Выбрано {{count}} из 1000","Selected groups":"Выбранные группы","Separate IDs with commas or newlines. Duplicates are removed. Clear the field to remove all allowlisted channels, then save settings.":"Разделяйте ID запятыми или переносами строк. Дубликаты удаляются. Чтобы очистить белый список, очистите поле и сохраните настройки.","Settings changed elsewhere. Reload before saving again.":"Настройки изменены в другом сеансе. Перезагрузите перед сохранением.","Total {{count}}":"Всего: {{count}}","Unavailable channel":"Недоступный канал","Unavailable group":"Недоступная группа","Upstream model guard":"Проверка моделей провайдера"},"vi":{"Actual upstream model":"Mô hình thượng nguồn thực tế","Add rule":"Thêm quy tắc","All detected requests on the same channel share one count across models and groups. A matching request resets the count; missing model names are skipped.":"Mọi yêu cầu được kiểm tra trên cùng kênh dùng chung bộ đếm cho tất cả mô hình và nhóm. Yêu cầu khớp đặt lại bộ đếm; yêu cầu thiếu tên mô hình được bỏ qua.","Allowed upstream models":"Mô hình thượng nguồn được phép","Allowlisted channel IDs":"ID kênh trong danh sách cho phép","Allowlisted channels skip detection, counting, records, disabling, and notifications.":"Kênh trong danh sách bỏ qua kiểm tra, đếm, ghi nhận, vô hiệu hóa và thông báo.","Available channels":"Kênh có thể chọn","Channel":"Kênh","Channel allowlist":"Danh sách kênh miễn kiểm tra","Channel disabled":"Kênh đã vô hiệu hóa","Channel status":"Trạng thái kênh","Complete groups and model names for rule {{number}}.":"Điền đầy đủ nhóm và tên mô hình cho quy tắc {{number}}.","Consecutive mismatch threshold":"Ngưỡng sai lệch liên tiếp","Consecutive mismatches":"Số lần sai lệch liên tiếp","Detected at":"Thời điểm phát hiện","Detection enabled":"Bật kiểm tra","Enter an integer from 1 to 100.":"Nhập số nguyên từ 1 đến 100.","Enter positive integer channel IDs.":"Nhập ID kênh là số nguyên dương.","Expected upstream models":"Mô hình thượng nguồn dự kiến","Failed to load channels":"Không thể tải kênh","Group":"Nhóm","Loading channels...":"Đang tải kênh...","Loading records...":"Đang tải bản ghi...","Loading settings...":"Đang tải cài đặt...","Model guard settings saved":"Đã lưu cấu hình kiểm tra mô hình","Model matching rules":"Quy tắc đối chiếu mô hình","Model mismatch records":"Bản ghi sai lệch mô hình","Next page":"Trang tiếp theo","No allowlisted channels":"Chưa có kênh miễn kiểm tra","No channels found":"Không tìm thấy kênh","No groups available":"Không có nhóm khả dụng","No model matching rules":"Chưa có quy tắc đối chiếu mô hình","No model mismatch records":"Chưa có bản ghi sai lệch mô hình","Not disabled":"Chưa vô hiệu hóa","Notification Center":"Trung tâm thông báo","One exact model name per line":"Mỗi dòng một tên mô hình chính xác","Previous page":"Trang trước","Refresh records":"Làm mới bản ghi","Reload settings":"Tải lại cấu hình","Remove":"Xóa","Remove {{channel}} from allowlist":"Xóa {{channel}} khỏi danh sách miễn kiểm tra","Remove rule":"Xóa quy tắc","Request failed":"Yêu cầu thất bại","Request model":"Mô hình yêu cầu","Retry channel search":"Thử lại tìm kênh","Rule {{number}}":"Quy tắc {{number}}","Rule enabled":"Bật quy tắc","Save settings":"Lưu cài đặt","Saving...":"Đang lưu...","Search":"Tìm kiếm","Search channels":"Tìm kênh","Select no more than 1000 channels.":"Chọn tối đa 1000 kênh.","Selected {{count}} of 1000":"Đã chọn {{count}} / 1000","Selected groups":"Nhóm đã chọn","Separate IDs with commas or newlines. Duplicates are removed. Clear the field to remove all allowlisted channels, then save settings.":"Phân cách ID bằng dấu phẩy hoặc xuống dòng. ID trùng lặp được loại bỏ. Xóa nội dung rồi lưu cài đặt để xóa toàn bộ kênh khỏi danh sách.","Settings changed elsewhere. Reload before saving again.":"Cấu hình đã được thay đổi ở nơi khác. Tải lại trước khi lưu.","Total {{count}}":"Tổng {{count}}","Unavailable channel":"Kênh không khả dụng","Unavailable group":"Nhóm không khả dụng","Upstream model guard":"Kiểm tra mô hình thượng nguồn"},"zh-CN":{"Actual upstream model":"实测上游模型","Add rule":"添加规则","All detected requests on the same channel share one count across models and groups. A matching request resets the count; missing model names are skipped.":"同一渠道的所有检测请求跨模型、跨分组共用计数。匹配请求清零；缺少模型名的请求跳过。","Allowed upstream models":"允许的上游模型","Allowlisted channel IDs":"渠道白名单 ID","Allowlisted channels skip detection, counting, records, disabling, and notifications.":"白名单渠道跳过检测、计数、异常记录、关闭和通知。","Available channels":"可选渠道","Channel":"渠道","Channel allowlist":"渠道白名单","Channel disabled":"渠道已关闭","Channel status":"渠道状态","Complete groups and model names for rule {{number}}.":"请补全规则 {{number}} 的分组和模型名称。","Consecutive mismatch threshold":"连续异常阈值","Consecutive mismatches":"连续异常次数","Detected at":"异常时间","Detection enabled":"启用检测","Enter an integer from 1 to 100.":"请输入 1 至 100 的整数。","Enter positive integer channel IDs.":"请输入正整数渠道 ID。","Expected upstream models":"预期上游模型","Failed to load channels":"渠道加载失败","Group":"分组","Loading channels...":"正在加载渠道...","Loading records...":"正在加载记录...","Loading settings...":"正在加载设置...","Model guard settings saved":"模型校验配置已保存","Model matching rules":"模型匹配规则","Model mismatch records":"模型异常记录","Next page":"下一页","No allowlisted channels":"暂无白名单渠道","No channels found":"未找到渠道","No groups available":"暂无可用分组","No model matching rules":"暂无模型匹配规则","No model mismatch records":"暂无模型异常记录","Not disabled":"未关闭","Notification Center":"通知中心","One exact model name per line":"每行一个完整模型名称","Previous page":"上一页","Refresh records":"刷新记录","Reload settings":"重新加载配置","Remove":"移除","Remove {{channel}} from allowlist":"将 {{channel}} 移出白名单","Remove rule":"移除规则","Request failed":"请求失败","Request model":"请求模型","Retry channel search":"重新加载渠道","Rule {{number}}":"规则 {{number}}","Rule enabled":"启用规则","Save settings":"保存设置","Saving...":"正在保存...","Search":"搜索","Search channels":"搜索渠道","Select no more than 1000 channels.":"最多可选择 1000 个渠道。","Selected {{count}} of 1000":"已选 {{count}} / 1000","Selected groups":"已选组","Separate IDs with commas or newlines. Duplicates are removed. Clear the field to remove all allowlisted channels, then save settings.":"使用逗号或换行分隔，重复 ID 自动去重。清空输入并保存设置可移除全部白名单渠道。","Settings changed elsewhere. Reload before saving again.":"配置已被其他会话修改，请重新加载后再保存。","Total {{count}}":"共 {{count}} 条","Unavailable channel":"不可用渠道","Unavailable group":"不可用分组","Upstream model guard":"上游模型校验"}};
function createGuardPage(sdk, ui, translations) {
  const { jsx, jsxs, Fragment } = sdk.modules["react/jsx-runtime"];
  const { useEffect, useState } = sdk.modules.react;
  const I18n = sdk.modules["react-i18next"];
  const namespace = "upstream-model-guard";
  const useTranslation = () =>
    I18n.useTranslation(namespace, { useSuspense: false });
  const base = "/api/extensions/upstream-model-guard";

  function unwrap(response) {
    const body = response?.data ?? response;
    if (body?.success === false)
      throw new Error(body.message || "Request failed");
    return body?.data ?? body;
  }

  function errorText(error, t) {
    return (
      error?.response?.data?.message || error?.message || t("Request failed")
    );
  }

  function channelLabel(channel, t) {
    return `${channel.name?.trim() || t("Unavailable channel")} (#${channel.id})`;
  }

  function ChannelAllowlist(props) {
    const { t } = useTranslation();
    const onReady = props.onReady;
    const [search, setSearch] = useState("");
    const [query, setQuery] = useState({ keyword: "", page: 1, revision: 0 });
    const [state, setState] = useState({
      loading: true,
      error: "",
      items: [],
      total: 0,
    });
    const [idsText, setIdsText] = useState(() =>
      props.selected.map((channel) => channel.id).join("\n"),
    );
    const [idsError, setIdsError] = useState("");
    useEffect(() => {
      onReady(!state.loading && !state.error && !idsError);
    }, [state.loading, state.error, idsError, onReady]);
    useEffect(() => {
      let active = true;
      onReady(false);
      setState((current) => ({ ...current, loading: true, error: "" }));
      ui.api()
        .get(`${base}/channels`, {
          params: { keyword: query.keyword, page: query.page, page_size: 50 },
          skipErrorHandler: true,
        })
        .then(unwrap)
        .then((data) => {
          if (!active) return;
          if (!Array.isArray(data.items))
            throw new Error(t("Failed to load channels"));
          setState({
            loading: false,
            error: "",
            items: data.items,
            total: Number(data.total || 0),
          });
        })
        .catch((error) => {
          if (active)
            setState({
              loading: false,
              error: errorText(error, t),
              items: [],
              total: 0,
            });
        });
      return () => {
        active = false;
      };
    }, [query, t, onReady]);

    function changeIds(value) {
      if (props.disabled) return;
      setIdsText(value);
      const parts = value.split(/[\s,，;；]+/u).filter(Boolean);
      if (
        parts.some(
          (part) =>
            !/^\d+$/.test(part) ||
            !Number.isSafeInteger(Number(part)) ||
            Number(part) <= 0,
        )
      ) {
        setIdsError("Enter positive integer channel IDs.");
        props.onChange(props.selected);
        return;
      }
      const ids = [...new Set(parts.map(Number))];
      if (ids.length > 1000) {
        setIdsError("Select no more than 1000 channels.");
        props.onChange(props.selected);
        return;
      }
      setIdsError("");
      props.onChange(
        ids.map(
          (id) =>
            props.selected.find((channel) => channel.id === id) ||
            state.items.find((channel) => channel.id === id) || {
              id,
              name: t("Channel"),
              status: 0,
            },
        ),
      );
    }

    function toggle(channel, checked) {
      if (props.disabled) return;
      if (!checked) {
        const selected = props.selected.filter(
          (item) => item.id !== channel.id,
        );
        setIdsText(selected.map((item) => item.id).join("\n"));
        props.onChange(selected);
        return;
      }
      if (
        props.selected.length >= 1000 ||
        props.selected.some((item) => item.id === channel.id)
      ) {
        return;
      }
      const selected = [...props.selected, channel];
      setIdsText(selected.map((item) => item.id).join("\n"));
      props.onChange(selected);
    }

    const pages = Math.max(1, Math.ceil(state.total / 50));
    return jsxs(ui.Panel, {
      className: "guard-allowlist",
      children: [
        jsxs("div", {
          className: "guard-panel-header",
          children: [
            jsx("h2", { children: t("Channel allowlist") }),
            jsx("span", {
              className: "guard-muted",
              children: t("Selected {{count}} of 1000", {
                count: props.selected.length,
              }),
            }),
          ],
        }),
        jsxs("div", {
          className: "guard-panel-content",
          children: [
            jsx("p", {
              className: "guard-muted guard-help",
              children: t(
                "Allowlisted channels skip detection, counting, records, disabling, and notifications.",
              ),
            }),
            jsxs("div", {
              className: "guard-field",
              children: [
                jsx("label", {
                  htmlFor: "guard-channel-ids",
                  children: t("Allowlisted channel IDs"),
                }),
                jsx(ui.Textarea, {
                  id: "guard-channel-ids",
                  value: idsText,
                  disabled: props.disabled,
                  rows: 3,
                  maxLength: 30000,
                  "aria-invalid": Boolean(idsError),
                  "aria-describedby":
                    "guard-channel-ids-help" +
                    (idsError ? " guard-channel-ids-error" : ""),
                  onChange: changeIds,
                }),
                jsx("p", {
                  id: "guard-channel-ids-help",
                  className: "guard-muted guard-help",
                  children: t(
                    "Separate IDs with commas or newlines. Duplicates are removed. Clear the field to remove all allowlisted channels, then save settings.",
                  ),
                }),
                idsError &&
                  jsx("p", {
                    id: "guard-channel-ids-error",
                    role: "alert",
                    className: "guard-error",
                    children: t(idsError),
                  }),
              ],
            }),
            jsx("div", {
              className: "guard-selected-channels",
              children: props.selected.length
                ? props.selected.map((channel) =>
                    jsxs(
                      "div",
                      {
                        className: "guard-selected-channel",
                        children: [
                          jsx("span", { children: channelLabel(channel, t) }),
                          jsx(ui.Button, {
                            label: t("Remove"),
                            ariaLabel: t("Remove {{channel}} from allowlist", {
                              channel: channelLabel(channel, t),
                            }),
                            disabled: props.disabled || Boolean(idsError),
                            onClick: () => toggle(channel, false),
                          }),
                        ],
                      },
                      channel.id,
                    ),
                  )
                : jsx("p", {
                    className: "guard-muted",
                    children: t("No allowlisted channels"),
                  }),
            }),
            jsxs("form", {
              className: "guard-channel-search",
              onSubmit: (event) => {
                event.preventDefault();
                if (!props.disabled) {
                  setQuery((current) => ({
                    keyword: search.trim(),
                    page: 1,
                    revision: current.revision + 1,
                  }));
                }
              },
              children: [
                jsxs("div", {
                  className: "guard-field",
                  children: [
                    jsx("label", {
                      htmlFor: "guard-channel-search",
                      children: t("Search channels"),
                    }),
                    jsx(ui.Input, {
                      id: "guard-channel-search",
                      value: search,
                      disabled: props.disabled,
                      maxLength: 200,
                      onChange: setSearch,
                    }),
                  ],
                }),
                jsx(ui.Button, {
                  label: t("Search"),
                  disabled: props.disabled || state.loading,
                  onClick: () =>
                    setQuery((current) => ({
                      keyword: search.trim(),
                      page: 1,
                      revision: current.revision + 1,
                    })),
                }),
              ],
            }),
            jsxs("div", {
              role: "group",
              "aria-label": t("Available channels"),
              className: "guard-channel-results",
              children: [
                state.loading
                  ? jsx("p", {
                      role: "status",
                      className: "guard-muted",
                      children: t("Loading channels..."),
                    })
                  : null,
                state.error
                  ? jsxs("div", {
                      className: "guard-channel-error",
                      children: [
                        jsx("p", {
                          role: "alert",
                          className: "guard-error",
                          children: state.error,
                        }),
                        jsx(ui.Button, {
                          label: t("Retry channel search"),
                          disabled: props.disabled,
                          onClick: () =>
                            setQuery((current) => ({
                              ...current,
                              revision: current.revision + 1,
                            })),
                        }),
                      ],
                    })
                  : null,
                !state.loading && !state.error && state.items.length === 0
                  ? jsx("p", {
                      className: "guard-muted",
                      children: t("No channels found"),
                    })
                  : null,
                !state.loading && !state.error
                  ? jsx("div", {
                      className: "guard-channel-options",
                      children: state.items.map((channel) => {
                        const checked = props.selected.some(
                          (item) => item.id === channel.id,
                        );
                        return jsx(
                          ui.Checkbox,
                          {
                            label: channelLabel(channel, t),
                            checked,
                            disabled:
                              props.disabled ||
                              Boolean(idsError) ||
                              (!checked && props.selected.length >= 1000),
                            onChange: (value) => toggle(channel, value),
                          },
                          channel.id,
                        );
                      }),
                    })
                  : null,
                jsxs("div", {
                  className: "guard-panel-footer",
                  children: [
                    jsx("span", {
                      className: "guard-muted",
                      children: t("Total {{count}}", { count: state.total }),
                    }),
                    jsxs("div", {
                      className: "guard-actions",
                      children: [
                        jsx(ui.Button, {
                          label: t("Previous page"),
                          disabled:
                            props.disabled || state.loading || query.page <= 1,
                          onClick: () =>
                            setQuery((current) => ({
                              ...current,
                              page: current.page - 1,
                            })),
                        }),
                        jsx("span", {
                          className: "guard-page-number",
                          children: `${query.page} / ${pages}`,
                        }),
                        jsx(ui.Button, {
                          label: t("Next page"),
                          disabled:
                            props.disabled ||
                            state.loading ||
                            query.page >= pages,
                          onClick: () =>
                            setQuery((current) => ({
                              ...current,
                              page: current.page + 1,
                            })),
                        }),
                      ],
                    }),
                  ],
                }),
              ],
            }),
          ],
        }),
      ],
    });
  }

  function RuleEditor(props) {
    const { t } = useTranslation();
    const label = t("Rule {{number}}", { number: props.index + 1 });
    const modelId = `model-guard-model-${props.rule.key}`;
    const upstreamId = `model-guard-upstream-${props.rule.key}`;
    const unavailable = props.rule.group_codes.filter(
      (code) => !props.groups.some((group) => group.code === code),
    );
    const groups = [...props.groups, ...unavailable.map((code) => ({ code }))];
    const changeGroup = (code, checked) =>
      props.onChange({
        group_codes: checked
          ? [...props.rule.group_codes, code]
          : props.rule.group_codes.filter((value) => value !== code),
      });
    return jsxs(ui.Panel, {
      role: "group",
      "aria-label": label,
      className: "guard-rule",
      children: [
        jsxs("div", {
          className: "guard-panel-header",
          children: [
            jsx("h3", { children: label }),
            jsxs("div", {
              className: "guard-actions",
              children: [
                jsx(ui.Switch, {
                  checked: props.rule.enabled,
                  disabled: props.disabled,
                  label: t("Rule enabled"),
                  onChange: (enabled) => props.onChange({ enabled }),
                }),
                jsx(ui.Button, {
                  icon: "Trash2",
                  label: t("Remove rule"),
                  iconOnly: true,
                  disabled: props.disabled,
                  onClick: props.onRemove,
                }),
              ],
            }),
          ],
        }),
        jsxs("div", {
          className: "guard-panel-content guard-rule-fields",
          children: [
            jsxs("fieldset", {
              className: "guard-groups",
              disabled: props.disabled,
              children: [
                jsx("legend", { children: t("Selected groups") }),
                groups.length
                  ? jsx("div", {
                      className: "guard-group-options",
                      children: groups.map((group) =>
                        jsx(
                          ui.Checkbox,
                          {
                            label: group.name?.trim() || group.code,
                            checked: props.rule.group_codes.includes(
                              group.code,
                            ),
                            disabled: props.disabled,
                            onChange: (checked) =>
                              changeGroup(group.code, checked),
                          },
                          group.code,
                        ),
                      ),
                    })
                  : jsx("p", {
                      className: "guard-muted",
                      children: t("No groups available"),
                    }),
              ],
            }),
            jsxs("div", {
              className: "guard-model-fields",
              children: [
                jsxs("div", {
                  className: "guard-field",
                  children: [
                    jsx("label", {
                      htmlFor: modelId,
                      children: t("Request model"),
                    }),
                    jsx(ui.Input, {
                      id: modelId,
                      value: props.rule.model,
                      disabled: props.disabled,
                      maxLength: 255,
                      onChange: (model) => props.onChange({ model }),
                    }),
                  ],
                }),
                jsxs("div", {
                  className: "guard-field",
                  children: [
                    jsx("label", {
                      htmlFor: upstreamId,
                      children: t("Allowed upstream models"),
                    }),
                    jsx(ui.Textarea, {
                      id: upstreamId,
                      value: props.rule.upstreamText,
                      disabled: props.disabled,
                      rows: 4,
                      placeholder: t("One exact model name per line"),
                      onChange: (upstreamText) =>
                        props.onChange({ upstreamText }),
                    }),
                  ],
                }),
              ],
            }),
          ],
        }),
      ],
    });
  }

  function Settings(props) {
    const { t } = useTranslation();
    const onGroupsLoaded = props.onGroupsLoaded;
    const [nextKey, setNextKey] = useState(1);
    const [reload, setReload] = useState(0);
    const [state, setState] = useState({
      loading: true,
      error: "",
      config: null,
      groups: [],
    });
    const [draft, setDraft] = useState({
      enabled: false,
      rules: [],
      threshold: "2",
      excludedChannels: [],
    });
    const [channelsReady, setChannelsReady] = useState(false);
    const [saving, setSaving] = useState(false);
    const [saveError, setSaveError] = useState("");
    const [saved, setSaved] = useState(false);
    const [conflict, setConflict] = useState(false);

    useEffect(() => {
      let active = true;
      setChannelsReady(false);
      setState((current) => ({ ...current, loading: true, error: "" }));
      Promise.all([
        ui.api().get(`${base}/config`, { skipErrorHandler: true }).then(unwrap),
        ui.api().get(`${base}/groups`, { skipErrorHandler: true }).then(unwrap),
      ])
        .then(([config, groups]) => {
          if (!active) return;
          setState({
            loading: false,
            error: "",
            config,
            groups: Array.isArray(groups) ? groups : [],
          });
          onGroupsLoaded(Array.isArray(groups) ? groups : []);
          setDraft({
            enabled: Boolean(config.enabled),
            threshold: String(config.failure_threshold ?? 2),
            excludedChannels: (config.excluded_channel_ids || []).map(
              (id) =>
                (config.excluded_channels || []).find(
                  (channel) => channel.id === id,
                ) || {
                  id,
                  name: "",
                  status: 0,
                },
            ),
            rules: (config.rules || []).map((rule, index) => ({
              key: index + 1,
              enabled: Boolean(rule.enabled),
              group_codes: [...rule.group_codes],
              model: rule.model,
              upstreamText: rule.upstream_models.join("\n"),
            })),
          });
          setNextKey((config.rules || []).length + 1);
          setConflict(false);
          setSaveError("");
        })
        .catch((error) => {
          if (active) {
            setState((current) => ({
              ...current,
              loading: false,
              error: errorText(error, t),
            }));
          }
        });
      return () => {
        active = false;
      };
    }, [reload, t, onGroupsLoaded]);

    function edit(patch) {
      setDraft((current) => ({ ...current, ...patch }));
      setSaved(false);
      setSaveError("");
    }

    function updateRule(key, patch) {
      edit({
        rules: draft.rules.map((rule) =>
          rule.key === key ? { ...rule, ...patch } : rule,
        ),
      });
    }

    function addRule() {
      edit({
        rules: [
          ...draft.rules,
          {
            key: nextKey,
            enabled: true,
            group_codes: [],
            model: "",
            upstreamText: "",
          },
        ],
      });
      setNextKey((value) => value + 1);
    }

    async function save() {
      if (saving || conflict || !state.config || !channelsReady) return;
      const threshold = Number(draft.threshold);
      if (!Number.isInteger(threshold) || threshold < 1 || threshold > 100) {
        setSaveError(t("Enter an integer from 1 to 100."));
        return;
      }
      if (draft.excludedChannels.length > 1000) {
        setSaveError(t("Select no more than 1000 channels."));
        return;
      }
      const rules = draft.rules.map((rule) => ({
        enabled: rule.enabled,
        group_codes: [...rule.group_codes],
        model: rule.model.trim(),
        upstream_models: [
          ...new Set(
            rule.upstreamText
              .split(/\r?\n/)
              .map((value) => value.trim())
              .filter(Boolean),
          ),
        ],
      }));
      const invalid = rules.findIndex(
        (rule) =>
          !rule.group_codes.length ||
          !rule.model ||
          !rule.upstream_models.length,
      );
      if (invalid >= 0) {
        setSaveError(
          t("Complete groups and model names for rule {{number}}.", {
            number: invalid + 1,
          }),
        );
        return;
      }
      setSaving(true);
      setSaved(false);
      setSaveError("");
      try {
        const config = unwrap(
          await ui.api().put(
            `${base}/config`,
            {
              expected_version: state.config.config_version,
              enabled: draft.enabled,
              failure_threshold: threshold,
              excluded_channel_ids: draft.excludedChannels.map(
                (channel) => channel.id,
              ),
              rules,
            },
            { skipErrorHandler: true },
          ),
        );
        setState((current) => ({ ...current, config }));
        // 保存后使用宿主返回的渠道名称回显手动填写的 ID。
        setDraft((current) => ({
          ...current,
          excludedChannels: current.excludedChannels.map(
            (channel) =>
              (config.excluded_channels || []).find(
                (item) => item.id === channel.id,
              ) || channel,
          ),
        }));
        setSaved(true);
      } catch (error) {
        const stale = error?.response?.status === 409;
        setConflict(stale);
        setSaveError(
          stale
            ? t("Settings changed elsewhere. Reload before saving again.")
            : errorText(error, t),
        );
      } finally {
        setSaving(false);
      }
    }

    if (state.loading) {
      return jsx(ui.Panel, {
        children: jsx("div", {
          className: "guard-state",
          role: "status",
          children: t("Loading settings..."),
        }),
      });
    }
    if (state.error) {
      return jsx(ui.Panel, {
        children: jsxs("div", {
          className: "guard-state",
          children: [
            jsx("div", {
              role: "alert",
              className: "guard-error",
              children: state.error,
            }),
            jsx(ui.Button, {
              icon: "RefreshCw",
              label: t("Reload settings"),
              onClick: () => setReload((value) => value + 1),
            }),
          ],
        }),
      });
    }
    return jsxs(Fragment, {
      children: [
        jsxs(ui.Panel, {
          children: [
            jsxs("div", {
              className: "guard-panel-header guard-config-toolbar",
              children: [
                jsx("h2", { children: t("Model matching rules") }),
                jsx(ui.Switch, {
                  checked: draft.enabled,
                  disabled: saving || conflict,
                  label: t("Detection enabled"),
                  onChange: (enabled) => edit({ enabled }),
                }),
                jsxs("div", {
                  className: "guard-actions",
                  children: [
                    jsx(ui.Button, {
                      icon: "Plus",
                      label: t("Add rule"),
                      disabled: saving || conflict,
                      onClick: addRule,
                    }),
                    jsx(ui.Button, {
                      icon: "Save",
                      primary: true,
                      label: saving ? t("Saving...") : t("Save settings"),
                      disabled: saving || conflict || !channelsReady,
                      onClick: save,
                    }),
                  ],
                }),
              ],
            }),
            jsxs("div", {
              className: "guard-panel-content guard-tolerance",
              children: [
                jsxs("div", {
                  className: "guard-field",
                  children: [
                    jsx("label", {
                      htmlFor: "guard-failure-threshold",
                      children: t("Consecutive mismatch threshold"),
                    }),
                    jsx(ui.Input, {
                      id: "guard-failure-threshold",
                      type: "number",
                      min: 1,
                      max: 100,
                      step: 1,
                      value: draft.threshold,
                      disabled: saving || conflict,
                      onChange: (threshold) => edit({ threshold }),
                    }),
                  ],
                }),
                jsx("p", {
                  className: "guard-muted guard-help",
                  children: t(
                    "All detected requests on the same channel share one count across models and groups. A matching request resets the count; missing model names are skipped.",
                  ),
                }),
              ],
            }),
            saveError &&
              jsx("div", {
                className: "guard-panel-content guard-error",
                role: "alert",
                children: saveError,
              }),
            conflict &&
              jsx("div", {
                className: "guard-panel-content",
                children: jsx(ui.Button, {
                  icon: "RefreshCw",
                  label: t("Reload settings"),
                  disabled: saving,
                  onClick: () => {
                    setSaved(false);
                    setReload((value) => value + 1);
                  },
                }),
              }),
            saved &&
              jsx("div", {
                className: "guard-panel-content guard-success",
                role: "status",
                children: t("Model guard settings saved"),
              }),
            draft.rules.length === 0 &&
              jsx("p", {
                className: "guard-state guard-muted",
                children: t("No model matching rules"),
              }),
          ],
        }),
        jsx(ChannelAllowlist, {
          selected: draft.excludedChannels,
          disabled: saving || conflict,
          onReady: setChannelsReady,
          onChange: (excludedChannels) => edit({ excludedChannels }),
        }),
        ...draft.rules.map((rule, index) =>
          jsx(
            RuleEditor,
            {
              rule,
              index,
              groups: state.groups,
              disabled: saving || conflict,
              onChange: (patch) => updateRule(rule.key, patch),
              onRemove: () =>
                edit({
                  rules: draft.rules.filter((item) => item.key !== rule.key),
                }),
            },
            rule.key,
          ),
        ),
      ],
    });
  }

  function Records(props) {
    const { t } = useTranslation();
    const [page, setPage] = useState(1);
    const [refresh, setRefresh] = useState(0);
    const [state, setState] = useState({
      loading: true,
      error: "",
      items: [],
      total: 0,
    });
    useEffect(() => {
      let active = true;
      setState((current) => ({ ...current, loading: true, error: "" }));
      ui.api()
        .get(`${base}/records`, {
          params: { page, page_size: 20 },
          skipErrorHandler: true,
        })
        .then(unwrap)
        .then((data) => {
          if (active) {
            setState({
              loading: false,
              error: "",
              items: data.items || [],
              total: Number(data.total || 0),
            });
          }
        })
        .catch((error) => {
          if (active) {
            setState((current) => ({
              ...current,
              loading: false,
              error: errorText(error, t),
            }));
          }
        });
      return () => {
        active = false;
      };
    }, [page, refresh, t]);
    const columns = [
      {
        key: "created_at",
        title: t("Detected at"),
        render: (row) => {
          const date = new Date(Number(row.created_at) * 1000);
          return Number.isNaN(date.getTime()) ? "-" : date.toLocaleString();
        },
      },
      {
        key: "channel",
        title: t("Channel"),
        render: (row) =>
          jsxs("div", {
            children: [
              jsx("span", { children: row.channel_name || "-" }),
              jsx("small", {
                className: "guard-muted guard-channel-id",
                children: `#${row.channel_id}`,
              }),
            ],
          }),
      },
      {
        key: "group",
        title: t("Group"),
        render: (row) =>
          row.group_name?.trim() ||
          props.groups
            .find((group) => group.code === row.group)
            ?.name?.trim() ||
          row.group ||
          "-",
      },
      {
        key: "model",
        title: t("Request model"),
        render: (row) => row.requested_model || "-",
      },
      {
        key: "expected",
        title: t("Expected upstream models"),
        render: (row) => (row.expected_upstream_models || []).join("\n") || "-",
      },
      {
        key: "actual",
        title: t("Actual upstream model"),
        render: (row) => row.actual_upstream_model || "-",
      },
      {
        key: "count",
        title: t("Consecutive mismatches"),
        render: (row) =>
          `${row.consecutive_mismatches ?? 1} / ${row.failure_threshold ?? 1}`,
      },
      {
        key: "disabled",
        title: t("Channel status"),
        render: (row) =>
          row.channel_disabled === false
            ? t("Not disabled")
            : t("Channel disabled"),
      },
    ];
    let content = jsx(ui.RecordsTable, { columns, items: state.items });
    if (state.loading) {
      content = jsx("div", {
        className: "guard-state",
        role: "status",
        children: t("Loading records..."),
      });
    } else if (state.error) {
      content = jsx("div", {
        className: "guard-state guard-error",
        role: "alert",
        children: state.error,
      });
    } else if (!state.items.length) {
      content = jsx("div", {
        className: "guard-state guard-muted",
        children: t("No model mismatch records"),
      });
    }
    const pages = Math.max(1, Math.ceil(state.total / 20));
    return jsxs(ui.Panel, {
      className: "guard-records",
      role: "region",
      "aria-label": t("Model mismatch records"),
      children: [
        jsxs("div", {
          className: "guard-panel-header",
          children: [
            jsx("h2", { children: t("Model mismatch records") }),
            jsx(ui.Button, {
              icon: "RefreshCw",
              label: t("Refresh records"),
              disabled: state.loading,
              iconOnly: true,
              onClick: () => setRefresh((value) => value + 1),
            }),
          ],
        }),
        content,
        jsxs("div", {
          className: "guard-panel-footer",
          children: [
            jsx("span", {
              className: "guard-muted",
              children: t("Total {{count}}", { count: state.total }),
            }),
            jsxs("div", {
              className: "guard-actions",
              children: [
                jsx(ui.Button, {
                  icon: "ChevronLeft",
                  label: t("Previous page"),
                  iconOnly: true,
                  disabled: state.loading || page <= 1,
                  onClick: () => setPage((value) => value - 1),
                }),
                jsx("span", {
                  className: "guard-page-number",
                  children: `${page} / ${pages}`,
                }),
                jsx(ui.Button, {
                  icon: "ChevronRight",
                  label: t("Next page"),
                  iconOnly: true,
                  disabled: state.loading || page >= pages,
                  onClick: () => setPage((value) => value + 1),
                }),
              ],
            }),
          ],
        }),
      ],
    });
  }

  function GuardContent() {
    const { t } = useTranslation();
    const [groups, setGroups] = useState([]);
    return jsx(ui.Shell, {
      title: t("Upstream model guard"),
      actions: jsx("a", {
        className: "guard-notification-link",
        href: ui.notificationPath,
        children: t("Notification Center"),
      }),
      children: jsxs("div", {
        className: "upstream-model-guard",
        children: [
          jsx(Settings, { onGroupsLoaded: setGroups }),
          jsx(Records, { groups }),
        ],
      }),
    });
  }

  return function UpstreamModelGuardPage() {
    const { i18n } = I18n.useTranslation();
    const [ready, setReady] = useState(false);
    useEffect(() => {
      for (const [locale, values] of Object.entries(translations)) {
        i18n.addResourceBundle(locale, namespace, values, true, false);
      }
      // 两套宿主使用不同的中文语言代码，别名只注册到插件命名空间。
      i18n.addResourceBundle("zhCN", namespace, translations.zh, true, false);
      i18n.addResourceBundle(
        "zhTW",
        namespace,
        translations["zh-TW"],
        true,
        false,
      );
      setReady(true);
    }, [i18n]);
    return ready ? jsx(GuardContent, {}) : null;
  };
}

const sdk = globalThis.__NEW_API_EXTENSION_NATIVE_SDK__;
if (!sdk || sdk.sdk !== "v1" || sdk.platform !== "default") {
  throw new Error("Native extension SDK v1 for default is unavailable.");
}
const { jsx, jsxs } = sdk.modules["react/jsx-runtime"];
const { Button } = sdk.modules["@/components/ui/button"];
const { Card } = sdk.modules["@/components/ui/card"];
const { Input } = sdk.modules["@/components/ui/input"];
const { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } =
  sdk.modules["@/components/ui/table"];
const { SectionPageLayout } = sdk.modules["@/components/layout"];

const ui = {
  api: () => sdk.modules["@/lib/api"].api,
  notificationPath: "/notification-center",
  Button(props) {
    return jsx(Button, {
      type: "button",
      variant: props.primary ? "default" : "outline",
      size: "default",
      disabled: props.disabled,
      onClick: props.onClick,
      "aria-label": props.ariaLabel || props.label,
      title: props.ariaLabel || props.label,
      children: props.label,
    });
  },
  Panel(props) {
    return (Array.isArray(props.children) ? jsxs : jsx)(Card, {
      ...props,
      className: `guard-panel ${props.className || ""}`,
    });
  },
  Switch(props) {
    return jsxs("label", {
      className: "guard-switch-label",
      children: [
        jsx("input", {
          type: "checkbox",
          role: "switch",
          className: "guard-toggle",
          checked: props.checked,
          disabled: props.disabled,
          onChange: (event) => props.onChange(event.target.checked),
        }),
        jsx("span", { children: props.label }),
      ],
    });
  },
  Checkbox(props) {
    return jsxs("label", {
      className: "guard-checkbox-label",
      children: [
        jsx("input", {
          type: "checkbox",
          className: "guard-checkbox",
          checked: props.checked,
          disabled: props.disabled,
          onChange: (event) => props.onChange(event.target.checked),
        }),
        jsx("span", { children: props.label }),
      ],
    });
  },
  Input(props) {
    return jsx(Input, { ...props, onChange: (event) => props.onChange(event.target.value) });
  },
  Textarea(props) {
    return jsx("textarea", {
      ...props,
      className: "guard-textarea",
      onChange: (event) => props.onChange(event.target.value),
    });
  },
  RecordsTable(props) {
    return jsx("div", {
      className: "guard-table-scroll",
      children: jsxs(Table, {
        children: [
          jsx(TableHeader, {
            children: jsx(TableRow, {
              children: props.columns.map((column) =>
                jsx(TableHead, { children: column.title }, column.key),
              ),
            }),
          }),
          jsx(TableBody, {
            children: props.items.map((item) =>
              jsx(
                TableRow,
                {
                  children: props.columns.map((column) =>
                    jsx(TableCell, { children: column.render(item) }, column.key),
                  ),
                },
                item.id,
              ),
            ),
          }),
        ],
      }),
    });
  },
  Shell(props) {
    return jsxs(SectionPageLayout, {
      children: [
        jsx(SectionPageLayout.Title, { children: props.title }),
        jsx(SectionPageLayout.Actions, { children: props.actions }),
        jsx(SectionPageLayout.Content, { children: props.children }),
      ],
    });
  },
};

export default createGuardPage(sdk, ui, guardTranslations);
