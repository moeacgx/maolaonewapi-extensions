# 扩展模块仓库维护规则

- 本仓库只维护模块源码、安装包与下载清单，不包含宿主 Go 实现、生产配置或凭据。
- 保留 new-api、QuantumNous 及来源作者的名称、版权与许可。
- 所有版本必须注明宿主最低版本、额外能力要求和验证边界，不将 SHA-256 描述为发布者签名。
- 已发布的 `published/<id>/<version>/` 不可改写或删除；修正必须使用新版本。
- 修改模块时同时修改对应文档，重建原生入口，再运行发布与校验脚本。
- 分组对用户显示名称，内部匹配/提交使用稳定 ID/code。Default 与 Classic 分别验证。
- 只有明确授权才发布仓库或 Release；不自动修改生产宿主或安装模块。
